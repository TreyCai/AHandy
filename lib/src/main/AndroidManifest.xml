<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.ahandy">

    <application
            android:allowBackup="true"
            android:label="@string/app_name"
            android:icon="@drawable/ic_launcher">
        <activity android:name=".activity.SimpleWebsiteActivity" />
        <activity android:name=".activity.SwipeRefreshWebsiteActivity" />
    </application>

</manifest>
